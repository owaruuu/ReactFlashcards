import LoginControls from "./LoginControls";

const LoginHeader = () => {
    return (
        <>
            <p className="save">saving</p>

            <LoginControls />
        </>
    );
};

export default LoginHeader;
