import LoginControls from "./LoginControls";

const LoginHeader = () => {
    return (
        <div className="loginControls">
            <LoginControls />
        </div>
    );
};

export default LoginHeader;
